{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Enemy",
  "description": "A creature the party fights. DELIBERATELY THREE WEIGHTS, because a fight made only of full stat blocks is a fight nobody finishes: most enemies are an AC, some hit points and two attacks, and that is the whole of them. Kayki: \"some enemies just have attacks, AC and HP, nothing else; some have 1 feature, some have more, and the bosses will have class and features.\"\n\nNOTHING HERE IS ROLLED BY THE APP. The numbers are what a DM reads off a card — this system's app is a tracker, not a referee (RULES.md). The one thing the app does with an enemy is put it on the board with its hit points already filled in.\n\nWHAT AN ENEMY DOES NOT HAVE: levels, ability score improvements, tricks with cooldowns, or a character sheet. It MAY have an `engine` if it is a special or a boss \u2014 a pool with a ceiling that the DM counts during the fight \u2014 because a boss wearing a class wears its engine too, and a number nobody can see is a number the DM tracks on their fingers. A boss BORROWS a handful of a class's features, written out here in full and each saying where it came from — which is the only place the class it wears is recorded, because that IS what wearing a class means. It never resolves against the class data: a boss is a one-off and a class is a twenty-level ladder.\n\nDEFENCE, AND IT IS DECIDED BY TIER (Kayki, 2026-08-11): **specials and bosses Parry; normal enemies do not.** Every enemy has an AC and an attack must meet or beat it; a `normal` then simply takes the hit, which is what keeps a room of six moving — six mobs each rolling a Parry is six more d20 rolls a round. A `special` or a `boss` carries a `parryDC` and Parries exactly as a player does (MECHANICS §1): a flat d20 against it, above takes nothing, on it takes half, below takes half again as much.\n\nIT HAS THE SIX ABILITIES, AT EVERY TIER (Kayki, 2026-08-11). A player forcing a save on a goblin is the commonest thing that happens in a fight, so it cannot be gated to the upper tiers — and the enemies here force saves on players, which would be one-way otherwise. They are MODIFIERS, not scores: nothing in this system rolls against a raw 14, and every use is the modifier (a save, a check, and Dexterity for initiative). The spread is by tier: a `normal` is below a player, a `special` is nearly one, a `boss` equals or beats one — because it is meant to be above their level.",
  "type": "object",
  "required": [
    "name",
    "tier",
    "ac",
    "hp",
    "attacks"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "kebab-case unique id; defaults to slug of name"
    },
    "name": {
      "type": "string"
    },
    "source": {
      "type": "string",
      "default": "Circus System"
    },
    "flavor": {
      "type": "string",
      "description": "One line: what it looks like across a dark ring. This is the first thing a DM reads out."
    },
    "image": {
      "type": "string",
      "description": "Top-down token art: a path in the repo (assets/enemies/<id>.jpg) or a URL. The figure on the board and the picture on the card both read it."
    },
    "tier": {
      "type": "string",
      "enum": [
        "normal",
        "special",
        "boss"
      ],
      "description": "The three weights.\n  normal  — an AC, some hit points, one or two attacks, and AT MOST one small trait. Several appear at once. Killed in a round or two.\n  special — a fight's second gear: more hit points, a signature move, one or two features, usually a condition it can inflict. One or two per fight.\n  boss    — carries a CLASS and a handful of its features plus unique moves of its own. One per fight, and the fight is about it."
    },
    "size": {
      "type": "string",
      "enum": [
        "Tiny",
        "Small",
        "Medium",
        "Large",
        "Huge"
      ],
      "default": "Medium",
      "description": "Drives how many squares its figure covers on the board: Large is 2, Huge is 3."
    },
    "kind": {
      "type": "string",
      "description": "What sort of thing it is, in this world's own words — 'performer', 'beast', 'rigging', 'crowd'. Free text on purpose: a creature-type table is a taxonomy nobody asked for."
    },
    "partyLevel": {
      "type": "string",
      "description": "The band of party levels this is written for, e.g. '1-3'. Not a CR: this system has no encounter-building maths and inventing one here would be inventing a rule."
    },
    "ac": {
      "type": "integer",
      "minimum": 5,
      "maximum": 25
    },
    "hp": {
      "type": "integer",
      "minimum": 1
    },
    "hpDice": {
      "type": "string",
      "description": "The dice the hit points come from, e.g. '4d8+8' — for a DM who prefers to roll them."
    },
    "speed": {
      "type": "integer",
      "minimum": 0,
      "default": 30
    },
    "otherSpeeds": {
      "type": "string",
      "description": "Anything besides walking: 'climb 30 ft', 'fly 40 ft (rigging only)'."
    },
    "abilities": {
      "type": "object",
      "additionalProperties": false,
      "description": "The six, as MODIFIERS. Everything that is rolled uses the modifier — a saving throw, an ability check, and Dexterity for initiative — so a score would be a number to look at and never use. Anything left out is +0, which is what makes a plain enemy plain.",
      "properties": {
        "Strength": {
          "type": "integer",
          "minimum": -5,
          "maximum": 10
        },
        "Dexterity": {
          "type": "integer",
          "minimum": -5,
          "maximum": 10
        },
        "Constitution": {
          "type": "integer",
          "minimum": -5,
          "maximum": 10
        },
        "Intelligence": {
          "type": "integer",
          "minimum": -5,
          "maximum": 10
        },
        "Wisdom": {
          "type": "integer",
          "minimum": -5,
          "maximum": 10
        },
        "Charisma": {
          "type": "integer",
          "minimum": -5,
          "maximum": 10
        }
      }
    },
    "saveProf": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "Strength",
          "Dexterity",
          "Constitution",
          "Intelligence",
          "Wisdom",
          "Charisma"
        ]
      },
      "description": "The saves it is GOOD at: add the proficiency-shaped bonus below to these. The lever that makes a boss shrug off the one trick the party keeps reaching for."
    },
    "prof": {
      "type": "integer",
      "minimum": 0,
      "maximum": 6,
      "default": 2,
      "description": "Added to the saves in `saveProf`. Two unless it is meant to be markedly better at them; this is not a level, it is a size."
    },
    "initMod": {
      "type": "integer",
      "minimum": -5,
      "maximum": 20,
      "description": "What it adds to initiative. OMIT IT and the Dexterity modifier is used, which is right for almost everything — this is for the odd creature that acts out of order."
    },
    "parryDC": {
      "type": "integer",
      "minimum": 3,
      "maximum": 20,
      "description": "Specials and bosses only. It Parries exactly as a player does — a flat d20 against this DC, lower is better, costing its reaction (MECHANICS §1). A normal enemy has none and simply takes the hit, which is what keeps a room of six moving."
    },
    "resist": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Damage types it takes half of."
    },
    "immune": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Damage types and conditions it ignores entirely."
    },
    "vulnerable": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Damage types it takes double of. The lever that makes a party's odd trick suddenly the right answer."
    },
    "senses": {
      "type": "string",
      "description": "Anything beyond ordinary sight: 'blindsight 30 ft', 'hears everything within 60 ft'."
    },
    "attacks": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "name",
          "toHit",
          "damage"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": [
              "melee",
              "ranged"
            ],
            "default": "melee"
          },
          "toHit": {
            "type": "integer",
            "description": "Added to the d20 against the target's AC."
          },
          "reach": {
            "type": "string",
            "description": "Melee: '5 ft'. Ranged: '20/60 ft'."
          },
          "damage": {
            "type": "string",
            "description": "Fixed dice, e.g. '1d6+2'. An enemy's damage does not scale with anything — it is written down."
          },
          "damageType": {
            "type": "string"
          },
          "note": {
            "type": "string",
            "description": "The rider, if it has one: a save, a condition, a shove."
          }
        }
      }
    },
    "multiattack": {
      "type": "string",
      "description": "One line saying how many attacks it makes on its turn and which. Omit for one attack."
    },
    "engine": {
      "type": "object",
      "description": "A POOL THE DM HAS TO COUNT. Kayki, 2026-08-14: \"theres no way to track grinsel engine in game… every boss that has an engine needs to have this type of feature.\" A boss that wears a class wears its engine too — Grinsel carries 5 Mayhem — and until this existed the only record of it was a sentence inside a feature, which means the DM counting on their fingers while running five other creatures.\n\nThis is the TRACKER half only: how the pool is gained and what it is spent on stays in the feature that describes it, because that is rules text and this is a number with a ceiling. Bosses and specials only — a `normal` is an AC, some hit points and two attacks, and that is the whole point of the tier.",
      "required": [
        "name",
        "cap"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "What the pool is called, in the creature's own words: 'Mayhem', 'Mirth', 'Strings'."
        },
        "cap": {
          "type": "integer",
          "minimum": 1,
          "maximum": 20,
          "description": "The most it can hold. A fight starts at none of it, like every engine in this system."
        },
        "note": {
          "type": "string",
          "description": "One line for the DM's screen on how it is gained and spent. The full rule belongs in the feature."
        }
      }
    },
    "features": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name",
          "description"
        ],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "uses": {
            "type": "string",
            "description": "'1 / combat', '1 / turn', 'passive' — the same vocabulary a class feature uses."
          },
          "description": {
            "type": "string"
          },
          "from": {
            "type": "string",
            "description": "BOSSES ONLY: the class this feature is borrowed from, named so the table can see what it is fighting."
          }
        }
      }
    },
    "tactics": {
      "type": "string",
      "description": "How to PLAY it — what it opens with, what it does when hurt, what it will not do. The half of a stat block that makes a fight feel like something, and the half most stat blocks leave out."
    },
    "narration": {
      "type": "string",
      "description": "In play: what the table sees and hears when it acts. No numbers."
    }
  }
}
